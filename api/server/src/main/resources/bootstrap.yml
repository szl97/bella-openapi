#-------------------------------------------------------
# 服务启动配置文件
#-------------------------------------------------------
# 重要说明
# 此配置文件在服务启动时生效，可用于放置连接配置中心前的配置
#-------------------------------------------------------
server:
  port: ${PORT:8080}
  dubbo-port: ${DUBBOPORT:11200}
  max-http-header-size: 10000
spring:
  application:
    name: bella-openapi
